# Design

Verify the existing dialog lifecycle on a private D-Bus with offscreen QML. Preserve portal-first opening and fallback. Only fix a reproduced application defect.

Use existing tests, fixture builders, runners and build conventions. No public API, dependency or architectural changes. Keep historical verification records intact and record corrected evidence in this cycle.
